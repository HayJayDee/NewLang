use crate::parser::Parser;


pub struct Visitor {
    parser: Parser,
}

impl Visitor {
    pub fn new(parser: Parser) -> Self {
        Self {
            parser
        }
    }

    pub fn visit() {
        
    }
}
